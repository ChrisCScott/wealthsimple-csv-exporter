javascript:(()=>{function cleanAmount(str){return parseFloat(str.replace(/[−\u2212\u2013\u2014]/g,"-").replace(/,/g,"").replace(/[^\d.-]/g,""),)}function formatDate(dateStr){const today=new Date();let date;if(dateStr==="Today"){date=today}else if(dateStr==="Yesterday"){date=new Date(today);date.setDate(today.getDate()-1)}else{date=new Date(dateStr);if(!/\d{4}/.test(dateStr)){date.setFullYear(today.getFullYear())}}const year=date.getFullYear();const month=String(date.getMonth()+1).padStart(2,"0");const day=String(date.getDate()).padStart(2,"0");return `${ year }-${ month }-${ day }`}const datePattern=/^(Today|Yesterday|\w+ \d{1,2}, \d{4})$/;const pageRoot=document.querySelector("main#main")||document.body;const dateHeaders=[...pageRoot.querySelectorAll("h1, h2, h3, h4, h5, h6")].filter((h)=>datePattern.test(h.innerText.trim()),);if(dateHeaders.length===0){alert("Could not find transactions on this page.\nMake sure you are on the Wealthsimple Transactions page and try again.",);return}const rows=[];const seen=new Set();function parseTransactionRow(el){if(!el||!el.innerText){return null}const text=el.innerText.trim();if(!text||text.includes("Pending")||!/\$/.test(text)){return null}const parts=[...el.querySelectorAll("p[data-fs-privacy-rule=\"unmask\"]")].map((p)=>p.innerText.trim()).filter(Boolean);if(parts.length===0){return null}const payee=parts[0];const amountPart=[...parts].reverse().find((part)=>/\$/.test(part));if(!payee||!amountPart){return null}const amount=cleanAmount(amountPart);if(Number.isNaN(amount)){return null}return{payee,amount,amountPart,text}}for(const header of dateHeaders){const currentDate=formatDate(header.innerText.trim());let candidate=header.parentElement.nextElementSibling;while(candidate){const nestedHeader=candidate.querySelector("h1, h2, h3, h4, h5, h6");if(nestedHeader&&datePattern.test(nestedHeader.innerText.trim())){break}const transaction=parseTransactionRow(candidate);if(transaction){const uid=`${ currentDate }|${transaction.payee }|${transaction.amount }|${transaction.amountPart }`;if(!seen.has(uid)){seen.add(uid);rows.push([currentDate,`"${transaction.payee.replace(/"/g,'""')}"`,transaction.amount].join(","),)}}candidate=candidate.nextElementSibling}}if(rows.length===0){const url=window.location.href;const dollarCount=(document.body.innerText.match(/\$/g)||[]).length;let msg="No completed transactions found.\n\n";if(!url.includes("wealthsimple.com")){msg+="You don't appear to be on Wealthsimple.\nNavigate to my.wealthsimple.com/activity first."}else if(dollarCount===0){msg+="No dollar amounts found on page.\nMake sure you're on the Activity page."}else{msg+="Tip: Scroll down to load more transactions before clicking.\n\nIf transactions are visible but not exporting, please report at:\ngithub.com/dizzlkheinz/wealthsimple-csv-exporter/issues"}alert(msg);return}const csvContent=`Date,Payee,Amount\n${rows.join("\n")}`;const blob=new Blob([csvContent],{type:"text/csv;charset=utf-8;"});const url=URL.createObjectURL(blob);const link=document.createElement("a");link.href=url;link.download="wealthsimple_activity.csv";document.body.appendChild(link);link.click();document.body.removeChild(link);URL.revokeObjectURL(url)})();